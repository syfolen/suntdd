
module suntdd {
    /**
     * 按钮信息配置
     */
    export interface IButtonCfg {
        /**
         * 按钮对象
         */
        button: Laya.Button;

        /**
         * 是否为一次性按钮
         */
        once: boolean;
    }
}