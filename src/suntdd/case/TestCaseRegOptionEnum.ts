
module suntdd {
    /**
     * 测试用例注册可选项枚举
     * export
     */
    export enum TestCaseRegOptionEnum {
        /**
         * 插入（优先执行）
         * export
         */
        INSERT,

        /**
         * 追加（默认）
         * export
         */
        APPEND
    }
}